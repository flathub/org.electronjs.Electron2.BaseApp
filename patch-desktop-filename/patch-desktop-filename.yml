name: patch-desktop-filename
buildsystem: simple
build-options:
  append-path: /usr/lib/sdk/golang/bin
build-commands:
  - go build -mod=vendor .
  - install -Dm755 patch-desktop-filename -t ${FLATPAK_DEST}/bin
sources:
  - type: dir
    path: src

  - go.mod.yml